/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  basePath: '/gym-buddies-lp',
  assetPrefix: '/gym-buddies-lp',
  images: {
    unoptimized: true,
  },
};

export default nextConfig;
